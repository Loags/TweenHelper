# Historical documentation archive

These files are no longer active plans or current release records. They are retained temporarily so their implementation history and previous publishing state remain recoverable.

Do not use archived files as current requirements, compatibility promises, catalog counts, release instructions, or resume points.

Active plans are indexed by the repository root `ROADMAP.md`.

The archive contains completed implementation and lifecycle trackers plus superseded review, release, Publisher Portal, CLI, telemetry, and serialized-recipe plans.